spring:
  liquibase:
    enabled: false
  application:
    name: BeautyShopPolya
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db:5432/beauty_polya
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
minio:
  bucket: images
  url: http://minio:9000
  accessKey: minioadmin
  secretKey: minioadmin